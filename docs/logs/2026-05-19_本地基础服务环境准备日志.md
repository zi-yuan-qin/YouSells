# 2026-05-19 本地基础服务环境准备日志

- 日期：`2026-05-19`
- 负责人：`秦梓源`
- 任务名称：`本地 MySQL / Redis 开发环境统一`
- 当前状态：`🟡 进行中（验证受本机 Docker 引擎状态影响）`

## 本次任务目标

- 统一团队本地基础服务启动方式
- 让后续数据库与缓存接入有稳定运行底座
- 降低成员因环境不熟悉导致的开发阻塞

## 本次已完成内容

- 明确当前阶段先统一 `MySQL + Redis`，暂不做整套正式部署
- 将项目 `docker-compose.yml` 调整为团队可复用版本
- 为 `MySQL` 增加初始化 SQL 自动挂载
- 为 `MySQL / Redis` 增加健康检查
- 将默认开发端口统一为项目专用端口：
  - `MySQL: 13306`
  - `Redis: 16379`
- 同步更新：
  - `.env.example`
  - `yousells-server/src/main/resources/application.yml`
- 新增文档：
  - `docs/10_本地基础服务启动指南.md`

## 这样调整的原因

- 避免成员本机已有 `3306 / 6379` 服务时直接冲突
- 避免每个人都靠口头说明配置环境
- 为后端后续接入真实持久化逻辑做准备

## 下一步动作

1. 在本机执行一次 `docker compose up -d` 验证
2. 校验 `MySQL` 初始化和 `Redis` 连通性
3. 更新根目录和子模块 README 中的启动说明
4. 后续准备团队共享测试环境方案

## 本次验证情况

- `docker compose version` 已确认可用
- 本机执行 `docker compose up -d` 时，Docker 引擎未连接成功
- 当前报错特征为：
  - `open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.`

这通常说明：

- `Docker Desktop` 尚未启动
- 或 Docker Engine 尚未就绪

因此本次配置文件与文档已完成，但容器启动验证需要在 Docker Desktop 正常运行后再执行一次
