# 嘉诚 开发日志

日期范围：`2026-05-20` 起
负责人：`嘉诚`

---

## 日志格式约定

每条日志包含：

```markdown
### YYYY-MM-DD HH:MM — 模块名 — 操作简述

- **状态**：开始 / 进行中 / 已完成 / 阻塞
- **改动文件**：
  - 新建：xxx.vue
  - 修改：xxx.vue
- **测试结果**：`npm run build` 通过 / 失败，自动化测试通过 / 失败
- **联调结果**：接口返回正常 / 异常，页面表现正常 / 有 bug
- **阻塞点**：无 / [描述]
- **下一步**：[描述]
```

---

## 开发记录

### 2026-05-20 — 开发规划建立

- **状态**：✅ 已完成
- **改动文件**：无（仅建立规划和日志文档）
- **说明**：已制定 FE-002 ~ FE-004 详细开发方案，待确认后开始执行

---

### 2026-05-20 18:30 — FE-002 登录页联调收口

- **状态**：✅ 已完成
- **改动文件**：
  - 修改：`src/views/login/LoginView.vue`
- **改动内容**：
  - 添加表单校验规则（账号密码必填，空着提交标红）
  - 提交前 `formRef.validate()` 校验，不通过不发请求
  - 按钮 `:disabled="authStore.loading"` 防重复提交
  - 表单项绑定 `prop` 属性使校验生效
- **测试结果**：
  - `npm run build` ✅ 通过
  - Playwright 4 个测试全部通过 ✅
- **联调结果**：待本地启动 Docker + 后端后验证完整登录流程
- **阻塞点**：无
- **下一步**：用户验收 FE-002，确认后进入 FE-003

---

### 2026-05-20 20:50 — 开发环境搭建 — 后端启动成功

- **状态**：✅ 已完成
- **改动文件**：无（环境配置）
- **环境配置内容**：
  - MySQL 8.4：初始化 data 目录，安装 Windows 服务 `MySQL84`，端口 13306，root/root
  - MySQL 数据库 `yousells` 已创建，schema + seed SQL 已执行
  - Redis：Windows 服务运行中（`Redis`）
  - JDK 21：`JAVA_HOME` 已永久设置
  - Maven 3.9.6：`MAVEN_HOME` 已永久设置
  - Spring Boot 后端：编译成功，启动于 `localhost:8080`
- **接口验证**：
  - `POST /api/auth/login` admin/admin123 → ✅ 返回 JWT + 用户信息
  - `POST /api/auth/login` member/member123 → ✅ 返回 JWT + 用户信息
- **联调结果**：
  - 前端 5173 → 后端 8080 登录流程通畅
  - 输入账号密码 → 点击登录 → 自动跳转 `/dashboard` ✅
  - Playwright 4 个测试全部通过 ✅
- **阻塞点**：无
- **下一步**：嘉诚验收 FE-002 完整登录流程，确认后进入 FE-003 看板模块

---

### 2026-05-20 21:00 — FE-003 首页看板组件化

- **状态**：✅ 已完成
- **改动文件**：
  - 新建：`src/components/dashboard/DashboardStatCards.vue`
  - 新建：`src/components/dashboard/DashboardTaskList.vue`
  - 新建：`src/components/dashboard/DashboardCustomerList.vue`
  - 新建：`tests/smoke/dashboard-overview.spec.ts`
  - 修改：`src/views/dashboard/DashboardView.vue`
- **改动内容**：
  - 拆出三个子组件：统计卡片、今日公共安排列表、重点客户列表
  - 子组件覆盖三种状态：加载中（显示 "-" 或 "加载中..."）、有数据、空数据（显示"暂无"）
  - DashboardView 新增 `error` 状态，接口失败时页面内显示"数据加载失败，请点击刷新重试"
  - stats 在 overview 为 null 时返回全 0 占位数组（避免卡片缺失）
- **测试结果**：
  - `npm run build` ✅ 通过（vue-tsc + vite）
  - Playwright 5 个测试全部通过 ✅
- **联调结果**：Dashboard API 返回正常，页面渲染 4 个统计卡片 + 2 个空数据列表区
- **阻塞点**：无
- **下一步**：嘉诚验收 FE-003，确认后进入 FE-004

---

### 2026-05-20 21:30 — FE-004 客户总表组件化

- **状态**：✅ 已完成
- **改动文件**：
  - 新建：`src/components/customer-list/CustomerFilterBar.vue`
  - 新建：`src/components/customer-list/CustomerTable.vue`
  - 新建：`src/components/customer-list/CustomerTagPanel.vue`
  - 新建：`tests/smoke/customer-list.spec.ts`
  - 修改：`src/views/customer/CustomerListView.vue`
- **改动内容**：
  - CustomerFilterBar：2 个输入框（关键词、来源平台）+ 2 个下拉（意向等级 A/B/C/D、当前阶段 8 种）+ 搜索/重置按钮
  - CustomerTable：8 列（客户编号、昵称、来源平台、意向等级、当前阶段、负责人、下次跟进、标签），标签列用 el-tag 渲染，空数据显示"无"
  - CustomerTable：el-pagination 分页，total > 0 才显示，`highlight-current-row` 行高亮
  - CustomerTagPanel：onMounted 调 api 获取真实标签，loading/empty/data 三态，点标签触发按标签名搜索
  - CustomerListView：引入三个子组件，新增 error 状态 + isUnauthorizedError 处理，行点击 router.push 跳详情，标签点击触发筛选
  - 移除旧内联模板（table-summary、mini-tag-row、EmptyStateCard）
- **测试结果**：
  - `npm run build` ✅ 通过（vue-tsc + vite）
  - Playwright 5 个测试全部通过 ✅（筛选栏、表格、标签、行跳转、重置）
  - 全量 14 个测试全部通过 ✅（FE-002/003/004 无回归）
- **联调结果**：Customer API 联调正常，筛选/分页/行跳转/标签均工作
- **阻塞点**：无
- **下一步**：嘉诚验收 FE-004，全部验收通过后进行 Code Review
