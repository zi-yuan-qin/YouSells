spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:13306}/${MYSQL_DATABASE:yousells}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:root}

app:
  jwt:
    secret: VGhpc0lzQURldlNlY3JldEtleUZvcllvdVNlbGxzSldUQXV0aDEyMzQ1Njc4OTA=
    access-token-expire-seconds: 43200
    issuer: yousells-server
  ai:
    llm:
      endpoint: https://api.openai.com
      api-key: ""
      model: gpt-4o
      temperature: 0.3
      max-retries: 1
      timeout-seconds: 30
    embedding:
      endpoint: https://api.openai.com
      api-key: ""
      model: text-embedding-3-small
    qdrant:
      host: localhost
      port: 6334
      api-key: ""
      collection-scripts: yousells_scripts
    mock:
      enabled: true
