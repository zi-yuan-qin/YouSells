<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yousells.modules.customer.mapper.CustomerTagRelationMapper">

    <select id="selectTagNamesByCustomerId" resultType="java.lang.String">
        SELECT t.tag_name
        FROM customer_tags t
        INNER JOIN customer_tag_relations r ON t.id = r.tag_id
        WHERE r.customer_id = #{customerId}
          AND t.is_deleted = 0
        ORDER BY t.sort_order
    </select>

    <select id="selectTagNamesByCustomerIds" resultType="com.yousells.modules.customer.mapper.CustomerTagRelationMapper$CustomerTagMapping">
        SELECT r.customer_id AS customerId, t.tag_name AS tagName
        FROM customer_tags t
        INNER JOIN customer_tag_relations r ON t.id = r.tag_id
        WHERE r.customer_id IN
        <foreach collection="customerIds" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
          AND t.is_deleted = 0
        ORDER BY t.sort_order
    </select>

</mapper>
